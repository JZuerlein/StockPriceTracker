{
  "_comment": "Whatever YOUR host demands at startup, so a bare `dotnet test` works with no user secrets and no environment setup. The keys below are illustrative — replace them. The per-fixture connection string is injected in-memory and overrides anything set here.",

  "Jwt": {
    "Key": "integration-test-signing-key-at-least-32-chars"
  }
}
